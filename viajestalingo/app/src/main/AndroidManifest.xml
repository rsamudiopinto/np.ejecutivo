<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.viajestalingo">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RemoteMySQLActivity" android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MovieListingActivity" android:label="@string/listaviajes" />
        <activity android:name=".MovieUpdateDeleteActivity" android:label="@string/detalle_viajes" />
        <activity android:name=".AddMovieActivity" android:label="@string/detalle_viajes"/>
        <activity android:name=".ListViajes" android:label="@string/view_diario"/>
    </application>

</manifest>